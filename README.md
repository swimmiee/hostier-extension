# Hostier Extension

Chrome extension for Hostier platform connections.

## Scripts

- `npm run dev`
- `npm run package:prod`
- `npm run verify`

Artifacts are written to `dist/hostier-extension.zip`.
